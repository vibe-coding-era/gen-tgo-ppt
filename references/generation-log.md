# V1.2 生成日志规则

## 何时需要

- `create`、`convert`、重大 `repair`：仅在 Brief 与模板/风格均确认后，于写样片或完整输出前必须创建。
- 轻微 `repair`：可使用简化日志，但必须记录来源、修改、检查与回滚点；若改变内容或视觉方向，按 V1.2 确认门补充记录。
- `check_only`：默认不创建；正式审计需要耐久证据时才创建，且保持源文件只读。
- `handoff`：不创建生成日志，只写移交摘要。

当前目录不可写时返回 `E_WORKDIR_READONLY`，不要把日志静默写到其他目录。

## 版本口径

- Skill 版本：`V1.2`。
- 排版安全版本：`V1`。
- 模板包版本：`V1.1`。
- Skill、模板包与排版安全版本独立演进，不得互相推导。
- 日志脚本生成的 V1.2 骨架仍须在写样片前补全本规则要求的确认证据；未填写字段不得视为已确认。

## 文件规则

- 文件名：`gen-tgo-ppt-生成日志-YYYYMMDD-HHMMSS.md`。
- 默认拒绝覆盖同名日志；可先用 `scripts/create_generation_log.py --dry-run` 预览。
- `create`、`convert`、`repair` 调用日志脚本时必须传入 `--source-authority current_turn|explicit_path|confirmed_workspace|confirmed_continuation`；缺失时返回 `E_INPUT_UNCONFIRMED` 且不得写文件。
- 最终回复给出日志路径，并在交付前更新结果和遗留问题。

## 必填内容

模式、执行意图证据、Skill/排版版本、时间、工作目录、已确认输入及归属、来源、场景、受众、目标与核心主张、规格、格式、处理模式、Logo、内容改写边界、图片资产来源策略、Brief 内容及确认依据、展示过的模板/风格预览、智能推荐（若有）、最终模板/风格/映射及确认依据、页纲与逐页表达类型/视觉载体/资产来源/风格约束/文本预算、样片决定、输出、检查命令与结果、渲染证据、失败码与恢复、通道分工及其真实性、SSOT（若适用）、用户批准例外和遗留问题。

## 模板骨架

```markdown
# gen-tgo-ppt 生成日志

- Skill 版本：V1.2
- 排版安全版本：V1
- 模板包版本：V1.1
- 任务模式：create / convert / repair / check_only
- 执行意图证据：
- 创建时间：
- 当前目录：
- 任务标题：
- 已确认输入与归属：
- 场景 / 受众 / 目标与核心主张：
- 规格 / 输出格式 / 页数范围：
- 处理模式 / Logo / 内容处理边界：
- 图片资产来源策略与限制：

## 澄清、Brief 与设计确认

- 缺字段与用户回答：
- Brief 内容与确认依据：
- 模板/风格预览与智能推荐（若有）：
- 最终模板 / 风格 / 分页面映射与确认依据：

## 页纲、表达合同与文本预算

- 逐页：目的、表达类型、视觉载体、资产来源、风格约束、核心内容、布局、预计行数、密度风险与拆页策略。

## 样片与用户决定

- 样片路径、预览方式、用户反馈。
- 确认态：confirmed / explicitly_skipped；跳过时记录用户原话或等价证据、理由、时间。

## 完整输出
## PPTX/HTML 检查与逐页复核
## 失败、重试与降级
## 通道分工与交接
## SSOT（若适用）
## 操作记录
## 遗留问题
```
