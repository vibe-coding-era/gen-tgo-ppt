interface:
  display_name: "TGO鲲鹏会 PPT 生成 Skill"
  short_description: "TGO/GTLC PPTX 与 HTML 的 V1.1 模式化生成、转换、修复和校验"
  default_prompt: "使用 $gen-tgo-ppt-skill 处理 TGO/GTLC 演示稿：先选择 check_only、repair、convert、create 或 handoff 模式，再按需加载规则；生成或重大修改时使用 V1 模板包和排版校验，普通任务单 Agent 执行，正式或高风险交付启用独立生成/风格/文字通道。"
policy:
  allow_implicit_invocation: true
